#include "VariableStore.hpp"

namespace des {

// STUB -- see the header. Task 6 replaces both files.
bool   VariableStore::has(const std::string&) const { return false; }
double VariableStore::get(const std::string&) const { return 0.0; }

}  // namespace des
